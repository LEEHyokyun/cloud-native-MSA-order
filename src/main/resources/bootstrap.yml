spring:
  application:
    name: order-service
  cloud:
    config:
      uri: http://localhost:8888
#  config:
#    import: optional:configserver:http://localhost:8888  #config server에서 관리 중인 설정 정보들 중 /order-service/default 요청 = user-service.yml
